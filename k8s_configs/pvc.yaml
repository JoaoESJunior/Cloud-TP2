apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-project2
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  selector:
    matchLabels:
      namespace: joaojunior
